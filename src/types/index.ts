export type IDeal = { 
    title: string, 
    description: string, 
    buttonText: string, 
    backgroundColor: string 
};

export type IPlan={
    title:string,
    description:string,
    image:string
}